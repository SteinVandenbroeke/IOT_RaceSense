# carla_pose.yaml

# Paths to your dataset (Update these if your relative paths are different)
path: ../car_detection/carla_dataset
train: images/train
val: images/val

# Classes (Just 1 class: the car)
names:
  0: car

# Keypoint Format: [number_of_keypoints, dimensions]
# 4 keypoints (wheels), 3 dimensions (x, y, visibility)
kpt_shape: [8, 3]